# UrbanSDK React JS Challenge

This project is a React application designed to visualize cards and their details. It leverages modern React patterns, react routinh, and WebGL-based map rendering.

## 🛠 Tech Stack

- **Core:** React 19, TypeScript, Vite
- **Routing:** React Router (Data APIs)
- **UI & Styling:** Chakra UI v3
- **Geospatial:** Mapbox GL JS, GeoJSON types

---

## 🚀 Getting Started

Follow these steps to run the project locally.

### Prerequisites

Ensure you have Node.js (v18+ recommended) installed on your machine.
You will need a Mapbox Access Token. Create an `.env` file in the root directory and add:

```env
VITE_MAPBOX_TOKEN=your_mapbox_public_token_here
```

### Installation

1. Clone the repository and navigate to the project folder.
2. Install the dependencies:
   ```bash
   npm install
   ```

### Development server

Start the local development server (Vite):

```bash
   npm run dev
```

The application will be available at `http://localhost:5173`
