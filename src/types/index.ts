export type MapTopics = Readonly<{
  id: string;
  label: string;
  queryType: string;
  imageUrl: string;
  description: string;
  subLabel: string;
  visible: string;
  dataUrl: string;
  configUrl: string;
}>;
